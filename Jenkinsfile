pipeline{
    agent any

    tools {
        maven "MAVEN3.9"
        jdk "JDK17"
    }

    stages{
        stage("Build Code"){
            steps{
                sh "echo 'Building the code via git webhook2.0...'"
            }
        }
    }
}